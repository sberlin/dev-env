---

- import_playbook: setup.yml

- name: install workstation
  hosts: all
  gather_facts: true
  become: yes
  become_user: root
  roles:
    - git
    - bash
    - executables
    - tmux
    - vim
    - gnome
    - onedrive

